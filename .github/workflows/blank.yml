name: Update badges

on:
  schedule:
    # Runs at 2am UTC
    - cron: "*/1 * * * *"
jobs:
  update-readme:
    name: Update Readme with badges
    runs-on: ubuntu-latest
    steps:
      - name: Badges - Readme
        uses: ferhangedik/badge-readme@main
        with:       
          CREDLY_USER: <hayrettin-ferhan-gedik> # optional, but default will use the same from github
